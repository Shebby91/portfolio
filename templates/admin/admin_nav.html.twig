<header class="navbar sticky-top bg-dark flex-md-nowrap p-0 shadow" data-bs-theme="dark">
<a class="navbar-brand col-12 col-md-3 col-lg-2 me-0 h4 text-white p-3 m-0" href="{{path('app_admin')}}">Portfolio</a>
<div class="d-flex w-100 justify-content-between py-2">
        <div class="d-flex align-items-center px-3 px-md-4">
            <span class="h4 m-0">{{title}}</span>
        </div>
    <ul class="navbar-nav d-none d-md-flex flex-row">
      <li class="nav-item text-nowrap">
        <a class="nav-link d-flex align-items-center px-3 px-md-4" href="{{path('app_logout')}}">
            Sign out
          </a>
      </li>
    </ul>
</div>
</header>