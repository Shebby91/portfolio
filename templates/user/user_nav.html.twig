<nav class="navbar container navbar-expand-md bg-body-tertiary w-100 sticky-top mx-0 mx-md-5 py-2 px-3 px-sm-4 px-md-3" data-bs-theme="dark">
      <a class="navbar-brand" href="{{path('app_user')}}">
        <h5 class="offcanvas-title">Portfolio</h5>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvas" aria-controls="offcanvas" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvas" aria-labelledby="offcanvasLabel">
        <div class="offcanvas-header border-bottom px-0 mt-2 mx-4 py-4">
          <h5 class="offcanvas-title" id="offcanvasLabel">Navigation</h5>
          <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
        </div>
        <div class="offcanvas-body d-flex flex-column justify-content-between px-4 pe-md-0">
          <ul class="navbar-nav flex-grow-1 justify-content-between">
            <ul class="navbar-nav flex-grow-1">
              <li class="nav-item"><a class="nav-link" href="{{path('app_user')}}">Start</a></li>
              <li class="nav-item"><a class="nav-link" href="{{path('app_user_album')}}">Album</a></li>
            </ul>
            <li class="nav-item d-flex align-items-center">
              <div class="dropdown pt-3 pt-md-0 d-flex flex-column-reverse d-md-block">
                    <a href="#" class="d-flex align-items-center link-body-emphasis text-decoration-none dropdown-toggle"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        <img {% if user.profileImg is not null %}src="{{user.profileImg.filePath}}" {% else
                            %}src="{{ asset('images/default_profile.jpg') }}" {% endif %} alt="" width="32" height="32"
                            class="rounded-circle me-2">
                        <strong>{{user.firstName}} {{user.lastName}}</strong>
                    </a>
                    <ul class="dropdown-menu text-small shadow mb-3 mb-md-0 mt-md-2">
                        <li><a class="dropdown-item" href="{{path('app_user_profile')}}">Profile</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{path('app_logout')}}">Sign out</a></li>
                    </ul>
                </div>
            </li>
          </ul>
        </div>
      </div>
    
  </nav>